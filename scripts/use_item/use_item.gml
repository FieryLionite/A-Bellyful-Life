/// @description use_item(item, zone)
/// @param item
/// @param  zone
function use_item(argument0, argument1) {

	var item = argument0;
	var zone = argument1;
	script_execute(item.script_use, zone);



}

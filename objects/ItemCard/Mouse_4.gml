if (!ControlEnv.screen_lock &&!item.is_template){
    xpre = x;
    ypre = y;
    //alarm[0] = 15;
    if (!item.in_stomach && !item.in_bowels && !item.is_template){
        with (ItemCard) dragging = false;
        dragging = true;
    }
}


dragging = false;
mouse_down = false;
/*zone = instance_place(x,y,DropZone);
if (zone != noone){
    if (!item.is_tool){
        if (!item.in_stomach && !item.in_bowels){
            item.character = global.dialogue_char;
            item.zone = zone;
            if (zone == global.zone_stomach)
                item.in_stomach = true;
            else
                item.in_bowels = true;
                
            push_food(item);
            with(ControlBackpack) refresh_backpack();
        }
    }
    else{
        use_item(item, zone);
    }
}

*/